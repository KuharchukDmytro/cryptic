import { searchIcon, sendIcon, twoCheckmarksIcon } from '@/assets/icons';
import { crypticLogo } from '@/assets/logos';

export const IconsEnum = {
  CRYPTIC_LOGO: crypticLogo,
  SEARCH_ICON: searchIcon,
  SEND_ICON: sendIcon,
  TWO_CHECKMARKS_ICON: twoCheckmarksIcon,
} as const;
